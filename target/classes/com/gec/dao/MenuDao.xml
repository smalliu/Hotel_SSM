<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gec.dao.MenuDao">

    <!--根据账号查询用户-->
    <select id="getRoleMenus" resultType="com.gec.domain.RoleMenu">
        select * from view_role_menu where roleId=#{roleId}
    </select>

</mapper>